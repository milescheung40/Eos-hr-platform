# 管理员端布局优化说明（MVP）

## 本次优化范围
- 仅针对管理员端中后台页面的布局、样式、前端交互细节进行优化。
- 保留现有功能模块、接口调用、数据库关联逻辑，不删除、不禁用、不改业务流程。
- 优化文件：
  - `index.html`
  - `styles.css`
  - `app.js`（仅工作台页签/滚动等前端交互）

## 参考基准与提炼规则
- 参考来源：
  - `EOS ｜ Index (2026_4_19 16：26：48).html`（工作台结构）
  - `2号人事部网页copy` 下 38 个管理员端页面（考勤、招聘、档案、组织、人事报表、薪资等）
- 提炼出的统一规范（仅布局与体验）：
  - 中后台高信息密度：浅灰背景、白色卡片、细线边框、紧凑控件间距。
  - 管理台主色：绿色系（与 2 号人事部常见主色一致）。
  - 列表页优先：搜索区 + 表格区 + 分页/状态操作固定节奏。
  - 侧栏导航分组明确，减少跨层级跳转成本。

## 已落地的布局优化点
- **全局视觉基线统一**
  - 调整主题变量为灰底+绿色主色，统一卡片、边框、控件圆角与间距。
  - 统一状态色（成功/警告/处理中）与表格 hover 风格。
- **工作台对齐优化**
  - 按参考页重构为：页签条（首页/工作台）+ 菜单面板 + 业务工单面板。
  - 工作台菜单模块文案、顺序、图标与参考页一致。
  - 修复内嵌 `div.panel` 被路由面板样式误隐藏的问题（确保模块可见）。
- **管理页一致性优化**
  - 员工、组织、企业、社保、合同、审批、发票、退休、政策、导入、报表、设置、AI 助手页面统一控件与信息密度。
  - 在 `index.html` 各面板增加优化注释，便于后续维护追溯。
  - 第二轮细化：非首页/工作台模块统一为“标题条 + 筛选工具条 + 表格区 + 底部操作区”的紧凑布局节奏，贴合 2 号人事部后台页面的信息组织方式。
  - 最终收口：在企业管理、社保、公积金、审批、发票、薪资上传、报表中心增加可演示的“指标条（metric strip）”，提升管理员驾驶舱感与汇报可读性。
  - 首页收口：移除“导师演示路径”等突兀说明型内容，改为运营看板与待办列表，首页信息组织更贴近 2 号人事部后台习惯。

## 核心要求对齐说明
- 对齐「管理员端优先」：仅管理员端布局优化，企业端业务未做结构性变更。
- 对齐「核心功能完整」：增减员、社保、合同、政策、社保计算器等能力均保留。
- 对齐「不改业务逻辑」：后端 API、鉴权、数据表与业务交互未做破坏性调整。
- 对齐「MVP可演示」：本次以可运行、可演示、可验收为目标，不做过度复杂定制。

## 新增模块说明
- 本次未新增复杂业务模块。
- 仅新增页面级布局注释与工作台交互适配（页签切换、滚动定位），属于体验层优化。

## 后续可迭代（预留，不在本次强制范围）
- AI 能力接口位继续沿用 `ai-assistant` 页面与后端路由扩展。
- 第三方政策接口/支付接口保留扩展位，暂不接入外部平台。
